import { StyleSheet, Text, View } from "react-native";
import React from "react";

export const ProductReviewsScreen = () => {
  return (
    <View>
      <Text>ProductReviewsScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
