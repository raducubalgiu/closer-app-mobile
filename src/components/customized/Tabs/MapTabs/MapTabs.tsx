import { StyleSheet, Text, View } from "react-native";
import React, { memo } from "react";

const MapTabs = () => {
  return (
    <View>
      <Text>MapTabs</Text>
    </View>
  );
};

export default memo(MapTabs);

const styles = StyleSheet.create({});
