import { StyleSheet, Text, View } from "react-native";
import React from "react";

export const SearchBookablesTab = ({ search }: { search: string }) => {
  return (
    <View>
      <Text>SearchBookablesTab</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
