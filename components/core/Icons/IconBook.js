import { Icon } from "@rneui/themed";
import React from "react";

export const IconBook = ({ sx }) => {
  return (
    <Icon
      name="shopping"
      type="material-community"
      color="white"
      size={20}
      containerStyle={sx}
    />
  );
};
