import { StyleSheet, Text, View } from "react-native";

type IProps = { postId: string };

export const CommentsSheet = ({ postId }: IProps) => {
  return (
    <View>
      <Text>commentsSheet</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
