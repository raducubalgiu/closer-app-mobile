import { StyleSheet, Text, View } from "react-native";
import React from "react";

export const ProductSheet = () => {
  return (
    <View>
      <Text>ProductSheet</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
