import { StyleSheet, Text, View } from "react-native";

type IProps = { postId: string };

export const MoreSheet = ({ postId }: IProps) => {
  return (
    <View>
      <Text>videoMoreSheet</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
