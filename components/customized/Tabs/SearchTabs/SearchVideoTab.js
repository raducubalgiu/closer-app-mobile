import { StyleSheet, Text, View } from "react-native";
import React from "react";

export const SearchVideoTab = () => {
  return (
    <View>
      <Text>SearchVideoTab</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
