import { StyleSheet, Text, View } from "react-native";
import React from "react";

type IProps = { userId: string };

export const VideosPortraitTab = ({ userId }: IProps) => {
  return (
    <View>
      <Text>VideosPortraitTab</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
